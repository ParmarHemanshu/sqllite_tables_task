name: sqllite_tables_task
description: A new Flutter SQLlite task.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  drift: ^1.0.1
  dartz: ^0.10.0
  equatable: ^2.0.3
  moor_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.5
  mockito: ^5.0.16
  flutter_lints: ^1.0.4
  flutter_bloc:
  moor_generator:
  get_it:

flutter:

  uses-material-design: true
  assets:
    - assets/